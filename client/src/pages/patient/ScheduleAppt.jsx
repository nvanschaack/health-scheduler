import React from 'react'

export default function ScheduleAppt() {
  return (
    <div>
      {/* when patient schedules an appt, we need to have access to the providers availability. */}
      {/* this page should have a form --> the provider, date, time */}
    </div>
  )
}
